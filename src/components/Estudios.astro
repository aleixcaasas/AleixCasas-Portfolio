<section id="estudios">

    <a href="#estudios"><h2 data-animation="center" class="title">Estudios</h2></a>



</section>

<style>

    section{
        margin-block: 50px;
    }

    a{
        text-decoration: none;
        color: #000;
    }

    .title{
        text-transform: uppercase;
        letter-spacing: 10px;
        padding-left: 10px;
        margin-block: 30px;
        font-weight: bold;
        padding-bottom: 7px;
    }

        /* ANIMATIONS
    –––––––––––––––––––––––––––––––––––––––––––––––––– */
    .menu[data-animation="center"] a::before {
        transform-origin: center;
    }

    .llista_categories li {
        position: relative;
        display: flex;
        padding: 5px;
    }

    h2 {
        position: relative;
        display: block;
        padding: 5px;
        font-weight: bold;
    }

    h2::before {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        height: 2px;
        background: #FF2424;
        border-radius: 8px;
        z-index: 1;
        transform: scaleX(0);
        transform-origin: left;
        transition: transform 0.5s ease-in-out;
    }

    h2:hover::before {
        transform: scaleX(1);
    }

</style>